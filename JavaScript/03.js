// funtion to add number and return the result

// first method

function addNumber(x, y) {
    return x + y;
}

console.log(addNumber(7, 6));
// output: 13

// Second Method

function addNumber(a, b) {
    return a - b;
}
let result = addNumber(8 - 0);
print(result);
